# Misc info

## `->` operator

Same as `.` i.e `a->b` is same as `a.b` but is used for pointers. If `a` is pointer then `a.b` is not valid code. we must use either `a->b` or `(*a)->b`. But mostly first one is used as it is cleaner than the latter one. But functionally both are same.
